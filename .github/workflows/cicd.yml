name: CICD

on:
  workflow_dispatch:
  
jobs:

  code:
    runs-on: ubuntu-latest
    steps:
      - name: clone the code
        run: echo "cloning the code"
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build using Docker
        run: echo "Building using Docker"
         
  test:
    runs-on: ubuntu-latest
   steps:
     - name: test the code
     run: echo "test the app"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploying on the machine
        run: echo "deploying the code"
